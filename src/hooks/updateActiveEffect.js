/**
 * Hook that runs when an active effect is updated
 * @return {void}
 */
export default function updateActiveEffect() {
  // Hooks.on('preUpdateActiveEffect', async (effect, changes) => {
    
  // });
}; 